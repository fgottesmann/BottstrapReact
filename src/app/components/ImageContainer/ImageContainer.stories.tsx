import React from 'react';
import ImageContainer from './ImageContainer';

export default {
  title: 'Component/ImageContainer',
  component: ImageContainer,
};

export const ImageCon = (): JSX.Element => <ImageContainer />;
